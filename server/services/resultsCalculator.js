// Placeholder — will calculate BMI, daily calories, etc.
const calculateResults = (metrics) => {
  return {
    bmi: null,
    dailyCalories: null,
    note: 'Placeholder — will be implemented with real formulas',
  };
};

module.exports = { calculateResults };
